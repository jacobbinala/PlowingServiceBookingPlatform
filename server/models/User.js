// Placeholder: replace with real Mongoose schema when implementing #23/#22
// Expected shape: email, password (hashed), phone, address: { street, city, postalCode }, role?
// module.exports = mongoose.model('User', userSchema);
